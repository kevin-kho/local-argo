apiVersion: v1
kind: Secret
metadata:
  name: repo-istio
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
type: Opaque
stringData:
  name: istio # Repository name for display
  project: default
  url: https://istio-release.storage.googleapis.com/charts
  type: helm
---
# apiVersion: networking.istio.io/v1alpha3
# kind: ServiceEntry
# metadata:
#   name: istio-release
#   namespace: argocd
# spec:
#   hosts:
#     - istio-release.storage.googleapis.com
#   location: MESH_EXTERNAL # Indicates it's outside the mesh
#   ports:
#     - number: 443
#       name: https
#       protocol: HTTPS
#   resolution: DNS
