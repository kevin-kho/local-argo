apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: alpine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alpine
  template:
    metadata:
      labels:
        app: alpine
    spec:
      containers:
        - name: alpine
          image: alpine/curl
          command: ["sleep", "infinity"]
